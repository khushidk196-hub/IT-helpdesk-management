# Implementation Prompt — As an Employee, I want to receive notifications when my support ticket changes so that I stay informed about progress and required actions; As an IT Support Agent, I want to receive notifications when a support ticket is assigned or reassigned so that I can act on owned work without delay; As an IT Support Agent, I want ticket lifecycle events to trigger notifications so that stakeholders are informed of ticket activity (+83 more)

> Use this prompt with any AI-assisted development tool to implement this feature.
> Generated by Astra Spec-Driven Development.

## Context

You are implementing the **As an Employee, I want to receive notifications when my support ticket changes so that I stay informed about progress and required actions; As an IT Support Agent, I want to receive notifications when a support ticket is assigned or reassigned so that I can act on owned work without delay; As an IT Support Agent, I want ticket lifecycle events to trigger notifications so that stakeholders are informed of ticket activity (+83 more)** feature.
Feature reference: **1518428762**.
Follow the specification exactly. Do not add features not described in the spec.

## Files to Reference

- **Project Context:** `specs/.devx/project-context.md`
- **Current State:** `specs/.devx/current-state.md`
- **Implementation Check:** `specs/.devx/implementation-check.md`
- **Feature Change Map:** `specs/.devx/change-maps/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more.md`
- **Golden Repo Guidelines:** `specs/.devx/guidance/golden-repo-guidelines.md` (REQUIRED when present)
- **Golden Repo Sources:** `specs/.devx/guidance/golden-repo-sources.md` (REQUIRED when present)
- **Golden UI Design System:** `specs/.devx/skills/ui-design/golden-ui-design-system.md` (REQUIRED when present)
- **Specification:** `specs/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more/specs.md`
- **Implementation Acceptance Checklist:** `specs/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more/requirements.md`


## Skill Files (Use In Your AI Prompt)

Use these skill files explicitly when implementing this feature:
- `specs/.devx/skills/implement/SKILL.md`
- `specs/.devx/skills/validate/SKILL.md`
- `specs/.devx/skills/next/SKILL.md`
- `specs/.devx/skills/autopilot/SKILL.md`
- `specs/.devx/skills/ui-design/SKILL.md`

## Work Items

- **[3690]** As an Employee, I want to receive notifications when my support ticket changes so that I stay informed about progress and required actions (3 pts)
  Acceptance:
1. When a support ticket submitted by an employee is created, assigned, updated with an employee-visible status change, resolved, or closed, the system generates exactly one notification for the requester for that event.
2. Each employee notification includes the ticket identifier, current ticket status, event timestamp, and an employee-safe activity summary, and excludes internal-only comments, assignment notes, or restricted troubleshooting details.
3. Notifications are sent only to the employee who requested the ticket and only when that employee is authorized to view the ticket at the time the notification is produced.
4. For every successfully generated employee notification, the system writes an audit entry containing ticket identifier, recipient user id, notification event type, delivery timestamp, and delivery outcome.
5. If notification generation fails for a supported ticket event, the ticket lifecycle update remains saved, the failure is logged with the event type and ticket identifier, and no restricted content is exposed in the failure path.
- **[3698]** As an IT Support Agent, I want to receive notifications when a support ticket is assigned or reassigned so that I can act on owned work without delay (3 pts)
  Acceptance:
1. System sends a notification to the assigned IT Support Agent when a support ticket is assigned or reassigned to that agent or queue.
2. System includes the support ticket identifier, priority, category, and assignment context needed to begin investigation promptly.
3. System updates assignment history and notification audit records together so reassignment events remain traceable for governance review.
4. System allows an authenticated IT Support Agent with assignment permission to assign an existing support ticket to an active IT Support Agent and saves the selected owner successfully
5. System updates the support ticket record to display the current assigned IT Support Agent immediately after assignment without losing existing ticket details or status information
6. System prevents assignment when the acting user lacks assignment permission or the selected assignee is not a valid active IT Support Agent and records the blocked attempt in the audit history
7. System allows an authorized IT Support Agent to assign or reassign a support ticket to a valid agent or queue and updates ownership without losing prior assignment history.
8. System prevents assignment actions by unauthorized roles or to invalid ownership targets and records no ownership change when validation fails.
9. System records each assignment or reassignment with ticket identifier, previous owner, new owner, actor, timestamp, and resulting status in the audit history for governance review.
10. System notifies the assigned IT Support Agent when assigned ticket activity occurs, including assignment, reassignment, comments, and status changes
11. System routes ticket activity notifications according to assignment and authorized support role participation so the correct operational recipient receives the update
12. System prevents agents from receiving or opening notifications for tickets outside their assigned or authorized access scope
- **[4123]** As an IT Support Agent, I want ticket lifecycle events to trigger notifications so that stakeholders are informed of ticket activity (5 pts)
  Acceptance:
1. When a ticket transitions through created, assigned, updated, resolved, or closed states, the system creates exactly one notification event per lifecycle action with the matching ticketId and eventType.
2. Each notification event payload includes ticketId, activityType, currentStatus, eventTimestamp, actorUserId, and intended recipient roles, and excludes attachment binaries, encrypted secrets, and comments not authorized for the recipient.
3. The notification event is persisted with a traceable reference to the originating ticket record and lifecycle action so that the event can be retrieved from ticket audit history.
4. If notification generation fails for a lifecycle action, the ticket lifecycle transaction remains committed, the notification event is marked failed with an error reason, and the failure is available for monitoring.
5. Only authenticated system components with permission to process ticket lifecycle actions can generate notification events, and unauthorized direct requests to create events are rejected with an access error.
- **[4131]** As an Employee, I want to receive ticket activity notifications through integrated channels so that I know when action or review is needed (3 pts)
  Acceptance:
1. System delivers ticket activity notifications to configured integration channels for the employee associated with the support ticket.
2. System sends notifications only for ticket activity the employee is authorized to view under access control policies.
3. System shows a clear delivery outcome for each ticket activity notification so notification failures can be identified and investigated.
4. User can receive ticket activity notifications through integrated channels so that I know when action or review is needed through the documented happy-path flow
5. System validates required inputs before completing "receive ticket activity notifications through integrated channels so that I know when action or review is needed"
- **[3701]** As an IT Manager, I want to receive notifications for SLA risk and breach events so that I can intervene before service targets are missed (3 pts)
  Acceptance:
1. When an open support ticket reaches a configured SLA warning threshold, the system generates exactly one SLA risk notification event for that threshold and ticket occurrence.
2. When an open support ticket passes its SLA target without resolution, the system generates an SLA breach notification that includes the ticket identifier, priority, category, assigned group or agent, and breach timestamp.
3. The notification recipient list is resolved using active routing rules based on ticket priority, category, current assignment, and escalation mapping, and notifications are not sent to inactive or unauthorized recipients.
4. Each SLA risk and breach notification event is stored with ticketId, eventType, triggeredAt, resolvedRecipients, delivery status, and correlation identifier for audit and reporting use.
5. If notification delivery to a target channel fails, the system records the failure status and error detail without preventing the SLA event from being logged for the affected ticket.
- **[3709]** As a System Administrator, I want to configure notification rules for support tickets so that timely notifications can be managed without application changes (5 pts)
  Acceptance:
1. System allows authorized administrators to configure notification triggers, recipient roles, and timing rules for support ticket lifecycle and SLA events.
2. System validates configuration changes before activation and preserves an audit trail of who changed notification rules, what changed, and when the change was applied.
3. System applies active notification rules consistently to new ticket events without exposing unauthorized ticket data in generated messages.
4. User can configure notification rules for support tickets so that timely notifications can be managed without application changes through the documented happy-path flow
5. System validates required inputs before completing "configure notification rules for support tickets so that timely notifications can be managed without application changes"
- **[3712]** As an IT Operations Analyst, I want to monitor notification delivery outcomes so that delayed or failed notifications can be identified and corrected quickly (5 pts)
  Acceptance:
1. The system provides a monitoring view that lists support ticket notification delivery outcomes with distinct states for sent, failed, and retried records.
2. Each notification record in the monitoring view displays the associated ticket identifier, notification event type, recipient, event timestamp, and current delivery state.
3. Authorized users can filter or search notification delivery records by at least delivery state, ticket identifier, and time range, and the results reflect only matching records.
4. The monitoring view is accessible only to authorized roles, and unauthorized users receive an access-denied response or do not see the feature.
5. Notification delivery telemetry is retained in persistent storage and remains available for operational investigation and audit reporting after the initial send attempt.
- **[3721]** As an IT Support Agent, I want the system to record ticket workflow changes automatically so that every critical support action is traceable (5 pts)
  Acceptance:
1. System automatically creates an audit record when a support ticket is created, assigned, reassigned, commented on, resolved, reopened, or closed
2. System stores each audit record with ticket identifier, actor identity, timestamp, workflow event type, and before-and-after values for changed ticket fields
3. System records audit events consistently across critical workflow states without allowing end users or support agents to bypass audit capture
4. System automatically creates an audit entry whenever a support ticket is created, updated, assigned, commented on, resolved, or closed.
5. System stores each audit entry with the support ticket identifier, authenticated actor, activity type, event timestamp, and before-and-after values for changed ticket fields where applicable.
6. System preserves audit entries as immutable records that cannot be changed through standard ticket update, assignment, comment, or status management functions.
- **[3724]** As an IT Manager, I want ticket audit records to be protected from alteration so that audit evidence remains trustworthy during reviews and investigations (3 pts)
  Acceptance:
1. When a critical ticket workflow event is captured, the stored audit record cannot be updated or deleted through standard ticket update, comment, assignment, or closure operations.
2. If a user without an authorized administrative permission attempts to modify or remove an audit record through UI or API, the system denies the request with an authorization error and leaves the original audit data unchanged.
3. Any permitted retention or archival operation applied to audit records creates its own separate audit entry containing the operator identity, timestamp, action type, and affected record reference.
4. Audit records remain available and unchanged after the related ticket is reassigned, escalated, resolved, or closed, with each lifecycle event appended as a new record rather than overwriting prior history.
5. Each protected audit entry stores the ticket identifier, workflow event type, actor, event timestamp, and change summary so that evidence remains complete and traceable for review activities.
- **[3732]** As a Compliance Reviewer, I want to view chronological ticket audit history so that I can verify how a support ticket moved through its lifecycle (3 pts)
  Acceptance:
1. When an authorized user opens audit history for a selected ticket, the system displays entries in chronological order using the recorded event timestamp.
2. Each audit history entry shows the workflow action, actor identity, event timestamp, and change summary for that ticket event.
3. The audit history view includes entries for assignment changes, status transitions, comments, resolution updates, and closure activity when those events exist for the selected ticket.
4. Users without the required role to view ticket audit history cannot access the history page or audit history API for the selected ticket.
5. If a ticket has no recorded audit events, the system shows an empty-state message for that ticket without exposing data from any other ticket.
- **[3739]** As a Compliance Reviewer, I want to filter ticket audit records by key criteria so that I can retrieve audit evidence efficiently for control testing and investigations (3 pts)
  Acceptance:
1. The system allows a Compliance Reviewer to submit audit history filters using any combination of ticket identifier, date range, actor, and workflow event type.
2. When a valid filter request is submitted, the system returns only audit records that satisfy all provided criteria and excludes non-matching ticket events.
3. Filtered audit results are displayed in chronological order based on audit event timestamp.
4. If the reviewer submits an invalid filter input, including an unsupported workflow event type or an end date earlier than the start date, the system rejects the request with a clear validation message and does not execute the search.
5. The system enforces role-based access on filtered audit retrieval and returns an access-denied response without exposing ticket workflow evidence when the requester lacks permission.
- **[3758]** As an IT Manager, I want immutable security audit history for ticket and attachment access so that compliance reviews and investigations can trace all sensitive actions (5 pts)
  Acceptance:
1. System records security-relevant events for support tickets and attachments, including access attempts, downloads, updates, assignments, comments, and status transitions, with actor, timestamp, action, and affected record reference
2. System stores audit history in an immutable or tamper-evident form and retains it according to organizational audit and record retention requirements for support operations
3. System allows only authorized personnel to review and export security audit records for ticket-related investigations and compliance reporting without permitting alteration of the original audit entries
4. System allows authorized roles to open a support ticket and view its audit history in chronological order with actor, action, timestamp, and change details.
5. System restricts access to ticket audit history based on role permissions and prevents unauthorized employees from viewing audit records for support tickets.
6. System presents ticket audit history with sufficient detail to support operational investigation, internal audit review, and record retention obligations for ticket-related activities.
- **[3746]** As an IT Manager, I want to control role-based access to support tickets and attachments so that sensitive help desk information is visible only to authorized personnel (5 pts)
  Acceptance:
1. System allows authenticated Business Users to access only the support tickets and attachments they created or are explicitly permitted to view, while IT Support Agents and IT Managers receive role-appropriate access based on assignment and administrative permissions
2. System prevents unauthorized viewing, updating, assigning, resolving, closing, commenting on, or downloading attachments for support tickets and returns an access-denied response without exposing ticket or attachment content
3. System enforces the same role-based authorization rules across ticket lifecycle actions, attachment access, and related API requests, with all permission decisions aligned to internal least-privilege and audit control policies
4. User can control role-based access to support tickets and attachments so that sensitive help desk information is visible only to authorized personnel through the documented happy-path flow
5. System validates required inputs before completing "control role-based access to support tickets and attachments so that sensitive help desk information is visible only to authorized personnel"
- **[3749]** As an IT Support Agent, I want support ticket data and attachments protected in transit and at rest so that sensitive issue information remains secure during handling (5 pts)
  Acceptance:
1. Support ticket fields, comment content, and attachment binary data are stored using approved enterprise encryption at rest, and decrypted values are returned only to authenticated users with permitted ticket access.
2. All browser requests, API calls, and internal service-to-service traffic for ticket creation, retrieval, update, comment entry, and attachment upload/download require TLS-secured transport, and non-secure HTTP requests are rejected or redirected according to security policy.
3. Application logs, error payloads, and monitoring events exclude raw ticket descriptions, comment bodies, attachment contents, encryption keys, and direct storage secrets while still recording request IDs, endpoint names, status codes, and failure reasons.
4. Attachment upload and download operations validate authorized access before any file transfer begins, and stored files remain encrypted in backing storage even when accessed through the application.
5. When encryption or secure transport controls fail, the system blocks the ticket or attachment operation, returns a controlled error response, and records a non-sensitive security event for operational investigation.
- **[3767]** Validate release test plan includes all critical workflow domains
- **[3768]** Validate entry and exit criteria are release-gating and specific
- **[3769]** Validate security and audit coverage is planned for negative paths
- **[3772]** Happy path validation — group critical workflow tests into execution suites
- **[3773]** As an IT Manager, I want critical support ticket workflows to be validated end to end so that operational, SLA, and audit control failures are detected before production use (5 pts)
  Acceptance:
1. A controlled end-to-end validation run covers ticket creation, assignment, investigation, resolution, and closure using only permitted status transitions, and any invalid transition is flagged as a failed validation result.
2. Validation data includes combinations of ticket category and priority that produce SLA target calculations, overdue states, and reporting outputs matching the configured SLA rules for those combinations.
3. Each validated workflow step records timestamped audit entries for ticket field updates, comments, assignment changes, and status transitions, and the validation evidence can be retrieved for review.
4. The validation process produces a pass or fail result for each workflow scenario with captured error details for failed checks, including the exact workflow step and rule that failed.
5. Only authorized management or test roles can execute or review workflow validation results, and unauthorized users are denied access without exposing workflow evidence data.
- **[3781]** As an IT Manager, I want to view a support performance dashboard so that I can monitor ticket operations and SLA adherence (5 pts)
  Acceptance:
1. Dashboard displays current counts and trends for ticket status, overdue tickets, agent workload, and SLA performance using standardized calculation rules
2. Dashboard allows filtering by date range, ticket category, priority, support queue, and assigned agent without exposing unauthorized ticket detail
3. Dashboard shows data refresh timestamp and loads dashboard views within the defined operational response target
4. System displays support performance dashboards with ticket status counts, overdue ticket counts, agent workload metrics, and SLA performance metrics using standardized support reporting rules
5. System allows the IT Manager to filter dashboard results by date range, ticket category, ticket priority, ticket status, and support queue while updating KPI totals consistently
6. System shows dashboard data currency with a last refreshed timestamp and returns dashboard results within 3 seconds for normal management review queries
7. System displays support performance dashboards for authorized management roles with ticket status, overdue item, agent workload, and SLA performance metrics calculated from Support Ticket, Assignment, and SLA records
8. System applies selected filters such as date range, queue, agent, category, and priority and refreshes dashboard results using consistent aggregation rules without exposing unauthorized ticket data
9. System records dashboard access and metric lineage in audit history so management review outputs can be traced to underlying support operations and governance controls
- **[3787]** As an IT Manager, I want to drill down from support summary metrics to team and agent performance so that I can identify workload imbalance and overdue ticket ownership (5 pts)
  Acceptance:
1. When an IT Manager selects a summary metric on the support operations dashboard, the system opens the next drill-down level with the same date range, queue, priority, category, and status filters applied automatically.
2. The drill-down hierarchy supports navigation from overall support metrics to queue-level, then team-level, then agent-level views without mixing results from unrelated organizational units.
3. Each detailed view displays, for the selected hierarchy level, ticket count, average aging, overdue ticket count, and SLA within-target versus breached totals using the same metric definitions as the originating summary metric.
4. Only users with authorized manager reporting access can open team and agent performance drill-down views; unauthorized users receive an access-denied response and no detailed operational data is returned.
5. Every successful access to team-level or agent-level performance detail writes an audit record containing requester identity, timestamp, dashboard view accessed, and active filter values.
- **[4011]** As a Business Manager, I want to drill down from support performance summaries to ticket-level details so that I can investigate performance exceptions (5 pts)
  Acceptance:
1. System allows the Business Manager to open ticket-level detail from summary metrics for overdue tickets, SLA breaches, ticket status totals, and agent workload counts
2. System preserves the selected report filters and reporting context during drill-down so that ticket-level records match the summary values shown in the management report
3. System displays ticket identifier, category, priority, status, assignment, age, and SLA information in the drill-down view while masking any restricted content the Business Manager is not authorized to view
4. User can drill down from support performance summaries to ticket-level details so that I can investigate performance exceptions through the documented happy-path flow
5. System validates required inputs before completing "drill down from support performance summaries to ticket-level details so that I can investigate performance exceptions"
- **[3796]** As an IT Manager, I want to generate support performance reports so that I can review trends and share operational results (3 pts)
  Acceptance:
1. System generates reports for ticket status distribution, overdue items, agent workload, and SLA performance for a selected reporting period
2. Reports can be filtered by category, priority, status, support queue, and assigned agent with consistent metric definitions across reports and dashboard views
3. Generated reports can be exported in an approved format with role-based controls applied to ticket and attachment-related data
4. System allows the IT Manager to export support performance reports in approved formats using the currently selected reporting filters and time period
5. System allows the IT Manager to schedule recurring report delivery for Business/IT Management recipients with configurable frequency and report scope
6. System applies access control and data protection rules to exported and scheduled reports so that unauthorized users do not receive restricted ticket details or attachment content
7. System allows authorized IT Manager roles to export support performance results using approved output formats while preserving the same filters and metric definitions used in management reporting views
8. System prevents export generation for unauthorized roles or non-permitted data scopes and enforces internal access control and data protection requirements for ticket-related information
9. System logs each export action with requester identity, timestamp, selected filters, and output type so management review exports are auditable and traceable
- **[3801]** As a Reporting Administrator, I want to configure support measurement rules so that reporting and monitoring follow approved SLA and governance policies (5 pts)
  Acceptance:
1. Only users with Reporting Administrator access can create or update support measurement rules, and unauthorized users are denied access to the configuration interface and save actions
2. The configuration form requires valid values for SLA target thresholds, overdue threshold definitions, and dashboard/report refresh parameters before a rule can be saved
3. When a measurement rule is updated, the system stores the new approved configuration and retains the immediately prior approved values for audit comparison
4. Each saved change writes an audit entry containing the acting user, timestamp, metric or report identifier, and field-level before/after values
5. Reporting and monitoring components read the latest approved measurement rules after save so subsequent dashboard calculations and report runs use the updated settings
- **[3810]** As an Information Security Auditor, I want monitoring and reporting access to be controlled and auditable so that sensitive ticket information remains protected (3 pts)
  Acceptance:
1. The system allows dashboard, report, and export access only for users whose roles include the required monitoring or reporting permissions
2. Users without the required permission receive an access-denied response and cannot view dashboard content, report results, or export data through direct URL or API access
3. Each dashboard view, report execution, and export action writes an immutable audit record with actor identity, timestamp, action type, and accessed dashboard or report identifier
4. Monitoring and reporting responses exclude or mask restricted ticket comments, attachment references, and other protected records for users whose role does not permit those details
5. All monitoring and reporting requests are served over authenticated sessions and protected transport so sensitive ticket information is not exposed in transit
- **[3818]** As an Employee, I want to submit an IT support ticket in a centralized platform so that my support request enters the managed support lifecycle (5 pts)
  Acceptance:
1. An authenticated employee can submit a new ticket only when issue description, category, and priority are provided, and the system creates one ticket record for the request.
2. On successful submission, the system generates a unique ticket identifier, sets the initial status to a newly created intake state, and returns the created ticket for status tracking.
3. If any required field is missing or invalid, the system blocks submission, shows field-level validation messages, and does not persist a ticket record.
4. If attachments are included, the system accepts only files that pass configured validation checks and stores them linked to the created ticket; invalid attachments prevent submission.
5. The system records the ticket creation event with actor, timestamp, submitted field values, and resulting initial status in auditable centralized records subject to access control.
- **[3826]** As an IT Support Agent, I want to process support tickets through controlled assignment and status transitions so that requests move consistently from creation to closure (8 pts)
  Acceptance:
1. System allows authorized IT Support Agents to assign, reassign, update, comment on, resolve, and close support tickets only through valid lifecycle transitions
2. System records every assignment, reassignment, comment, and status transition with the responsible actor and timestamp to preserve complete audit history
3. System prevents resolution or closure actions when required workflow conditions are not met, including insufficient update history or invalid transition state
4. The system allows an IT Support Agent to view a centralized list of tickets and open a ticket detail page showing description, category, priority, status, assignee, attachments, and history.
5. The system allows an IT Support Agent to assign or reassign a ticket only to eligible support personnel and records the assignment change in the ticket activity history.
6. The system allows an IT Support Agent to update ticket information, including category, priority, status, and resolution details, with validation for required fields and allowed values.
7. The system allows an IT Support Agent to add comments to a ticket, stores each comment with author and timestamp, and displays comments in chronological order on the ticket record.
8. The system enforces valid lifecycle transitions across creation, assignment, investigation, resolution, and closure, and prevents resolution or closure when required transition conditions are not met.
- **[3835]** As an IT Manager, I want secure audit trails and support dashboards so that ticket handling remains compliant, reviewable, and measurable (8 pts)
  Acceptance:
1. When an IT Manager opens the support dashboard, the system displays aggregated metrics for ticket status, overdue tickets, agent workload, and SLA performance using the same calculation rules applied across all dashboard widgets and report outputs
2. The system records an immutable audit entry for each ticket create, status change, assignment change, comment addition, and attachment-related action with ticketId, eventType, actorUserId, actorRole, timestamp, and before/after values where applicable
3. Users without an authorized managerial or governance role cannot access support dashboards, exports, or audit history endpoints and receive a forbidden response without exposure of ticket or metric data
4. Audit history for a selected ticket can be retrieved in chronological order and includes enough detail to reconstruct who changed what, when the change occurred, and the resulting ticket state
5. Dashboard and report data excludes tickets outside the viewer's authorized scope while still returning accurate totals, overdue counts, workload measures, and SLA metrics for the permitted dataset
- **[4184]** As a user, I want to use Create Support Ticket so that I can benefit from its core functionality (5 pts)
  Acceptance:
1. Feature functionality works as described
2. User can access the feature through the UI
3. Feature handles edge cases gracefully
4. Feature is properly tested
5. Feature meets performance requirements
- **[4193]** As a user, I want to configure Create Support Ticket settings so that I can customize it to my needs (3 pts)
  Acceptance:
1. An authenticated user can open Create Support Ticket settings and view either their previously saved preferences or system defaults when no preference record exists.
2. When the user saves valid settings for defaultCategoryId, defaultPriorityCode, expandDetailsOnLoad, and showAttachmentPanelOnLoad, the system persists the values and confirms the save without creating duplicate preference records for the same user and feature.
3. If the user submits an inactive category, unsupported priority, or invalid data type for a boolean setting, the system rejects the save and displays a field-specific validation message while preserving the user's other entered values.
4. When the same user opens the Create Support Ticket form after saving settings, the form initializes using the saved preferences, and any missing or no-longer-valid preference falls back to the approved system default.
5. A user cannot retrieve or update another user's Create Support Ticket settings through the UI or API, and unauthorized access attempts return the appropriate access-denied response.
- **[3844]** As an Employee, I want to enter issue description, category, and priority when creating a support ticket so that IT Support Agents receive the information needed to process my request (3 pts)
  Acceptance:
1. System requires issue description, category, and priority during ticket creation and creates the support ticket only when all required values are provided
2. System stores the submitted issue description, category, and priority against a unique support ticket record for downstream assignment and SLA processing
3. System records the authenticated employee identity and ticket creation timestamp in the audit history to support internal access control and record retention requirements
4. User can enter issue description, category, and priority when creating a support ticket so that IT Support Agents receive the information needed to process my request through the documented happy-path flow
5. System validates required inputs before completing "enter issue description, category, and priority when creating a support ticket so that IT Support Agents receive the information needed to process my request"
- **[3847]** As an Employee, I want to add attachments during ticket creation so that I can provide supporting evidence for my support request (3 pts)
  Acceptance:
1. The ticket creation form allows an authenticated employee to select one or more attachments before submission and persists accepted files with the newly created ticket record.
2. The system accepts only configured file types and rejects files with disallowed extensions, empty content, or size above the upload limit with a file-specific validation message.
3. If one or more selected files fail validation, the employee can remove the rejected files and still submit the ticket with the remaining valid attachments without losing entered ticket details.
4. Uploaded attachments are stored with metadata including ticketId, originalFileName, contentType, fileSize, uploadedBy, and uploadedAt, and are retrievable only by authorized users for that ticket.
5. Each successful and failed attachment upload attempt during ticket creation is recorded in the audit trail with the employee identifier, ticket identifier when created, file name, outcome, and timestamp.
- **[3855]** As an Employee, I want to view the current status of my submitted support ticket so that I can track progress without contacting the help desk (3 pts)
  Acceptance:
1. System displays the current status of each support ticket submitted by the authenticated employee using approved lifecycle values such as creation, assignment, investigation, resolution, and closure
2. System prevents an employee from viewing the status of another employee's support ticket and enforces authenticated access in accordance with internal access control policies
3. System shows ticket identifier, category, priority, and current status together on the ticket view so the employee can understand the state of the submitted request
4. System displays ticket status history in chronological order with the date and time of each recorded transition after the ticket is submitted
5. System reflects only valid controlled lifecycle transitions in the status history so the ticket trail remains consistent with help desk workflow rules and audit expectations
6. System presents current status and historical status changes consistently across the ticket detail view without exposing restricted internal-only notes or unauthorized user information
7. The support ticket submission form displays an active ticket category list sourced from the configured category master data and allows selection of exactly one category before submission.
8. If the employee attempts to submit a support ticket without choosing a category, the form blocks submission and shows a field-level validation message stating that ticket category is required.
9. When a ticket is successfully created, the selected category is persisted on the ticket record and returned in the create-ticket response payload and ticket detail view.
10. The category selector excludes inactive ticket categories so employees cannot assign deprecated classification values to new tickets.
11. If the category service or lookup retrieval fails during form load or submission, the system prevents ticket creation with an invalid category and shows an actionable error message without losing the user-entered ticket details.
- **[3873]** As an IT Support Agent, I want to open a support ticket and view its full details so that I can understand the issue and continue investigation or resolution (3 pts)
  Acceptance:
1. An authorized IT Support Agent can open a selected ticket and view its description, category, priority, status, requester information, assignment details, comments, and status history in a single detail view.
2. If the authenticated user does not have permission to view the selected ticket, the system blocks access, returns an authorization error, and does not expose ticket fields, comments, attachment information, or status history.
3. The ticket detail response includes the correct data for the selected ticket identifier and does not mix comments, history, or assignment data from any other ticket.
4. Each successful and denied ticket detail view creates an immutable audit log entry containing ticket identifier, user identity, timestamp, action type, and access outcome.
5. When a ticket identifier does not exist, the system returns a not-found response and displays a non-sensitive error state without revealing whether other restricted tickets are present.
- **[3866]** As an IT Support Agent, I want to view the list of support tickets available to me so that I can identify and manage my assigned and accessible workload (3 pts)
  Acceptance:
1. The ticket list displays only tickets the authenticated IT Support Agent is authorized to access based on assignment or role-based visibility rules, and each row includes ticket ID, issue summary, category, priority, status, assigned agent, and created timestamp.
2. If a user without the required IT Support Agent ticket-view permission requests the ticket list, the system denies access with an authorization error and does not return any ticket metadata.
3. When no accessible tickets exist for the authenticated IT Support Agent, the interface shows an empty-state message instead of a blank grid or system error.
4. The ticket list request completes within 2 seconds for normal workload volumes while returning records sorted by most recently created ticket first.
5. Each successful and denied ticket list access attempt creates an audit entry containing the requesting user identity, action type, timestamp, and access outcome.
- **[3881]** As an IT Manager, I want to review ticket assignment history so that ticket ownership changes remain auditable and compliant (3 pts)
  Acceptance:
1. When a ticket is assigned or reassigned, the system stores an audit entry containing ticket identifier, action type, acting user, event timestamp, new owner, and previous owner when one exists.
2. When an authorized IT Manager opens assignment history for a ticket, the system returns only events for that ticket and displays them in chronological order.
3. If a user without assignment audit permission requests assignment history, the system denies access and does not expose any assignment event data.
4. Assignment history entries are read-only to end users and cannot be updated or deleted through the ticket interface or assignment history endpoint.
5. Each displayed assignment history row shows enough detail to identify the ownership transition, including prior owner, new owner, acting user, timestamp, and assignment action type.
- **[3889]** As an IT Support Agent, I want to update ticket information during processing so that the support ticket reflects the latest investigation details (5 pts)
  Acceptance:
1. System allows an authenticated IT Support Agent to update permitted support ticket fields such as category, priority, status notes, comments, and assignment-related information while the ticket is in a processing state
2. System validates required and formatted ticket fields during update and prevents saving invalid values while preserving the previously saved ticket information
3. System saves successful ticket updates immediately and displays the latest ticket information to the IT Support Agent without requiring ticket recreation
4. User can update ticket information during processing so that the support ticket reflects the latest investigation details through the documented happy-path flow
5. System validates required inputs before completing "update ticket information during processing so that the support ticket reflects the latest investigation details"
- **[3892]** As an IT Support Agent, I want ticket updates to follow controlled status and field rules so that processing remains compliant with support governance and SLA policies (5 pts)
  Acceptance:
1. When an IT Support Agent submits a ticket update, the system saves the change only if the ticket's current status is configured as editable and rejects the request for closed or restricted statuses.
2. The system validates requested status changes against the configured allowed transition rules and returns a clear error when an agent attempts a disallowed transition.
3. The system validates category and priority values against configured active options and rejects any update containing inactive, unknown, or unauthorized values.
4. If an update request contains a disallowed combination of fields, such as a status value that conflicts with other governed ticket fields, the system rejects the entire update and persists no partial changes.
5. For a valid update request, the system persists the approved field changes and returns the refreshed ticket state showing the final saved status, category, and priority values.
- **[3900]** As an IT Manager, I want all ticket information updates to be auditable so that support processing changes can be reviewed for compliance and operational oversight (3 pts)
  Acceptance:
1. System records each successful support ticket update with ticket identifier, agent identity, timestamp, changed field names, and previous and new values in an immutable audit history
2. System makes ticket update history available to authorized reviewers in chronological order so that support processing changes can be traced end to end
3. System retains ticket update audit records in accordance with internal audit and record retention requirements without exposing them to unauthorized roles
4. User can I want all ticket information updates to be auditable so that support processing changes can be reviewed for compliance and operational oversight through the documented happy-path flow
5. System validates required inputs before completing "I want all ticket information updates to be auditable so that support processing changes can be reviewed for compliance and operational oversight"
- **[3903]** As an IT Security Administrator, I want ticket update access to be restricted by role so that sensitive support ticket information is protected during processing (3 pts)
  Acceptance:
1. System permits ticket field updates only when the authenticated user holds a role configured for ticket update access, such as IT Support Agent or another explicitly permitted role, and rejects all other roles before any ticket data is changed.
2. When an unauthorized user attempts to update a ticket, the system returns an access denied response, leaves the ticket unchanged, and records an audit entry containing user identity, ticket identifier, action attempted, denial outcome, and timestamp.
3. All ticket update requests require an authenticated session and are processed only over secure transport; unauthenticated requests are rejected without exposing ticket content or editable field values.
4. Authorized users can successfully update allowed ticket information and the persisted changes are visible on subsequent ticket retrieval for the same ticket identifier.
5. Role enforcement is applied consistently across ticket update entry points so that direct API calls cannot bypass the same update restrictions enforced in the application workflow.
- **[3910]** As an IT Support Agent, I want to add comments to tickets so that I can document investigation updates and resolution context (3 pts)
  Acceptance:
1. System allows an IT Support Agent to add a comment to an existing support ticket and saves the comment to that ticket's history
2. System displays the newly added comment in the ticket comment history with the agent name and timestamp after successful submission
3. System prevents comment submission when the comment field is blank or contains only whitespace and shows a validation message
4. User can add comments to tickets so that I can document investigation updates and resolution context through the documented happy-path flow
5. System validates required inputs before completing "add comments to tickets so that I can document investigation updates and resolution context"
- **[3916]** As an IT Support Agent, I want comment access to follow ticket permissions so that sensitive ticket information remains protected (3 pts)
  Acceptance:
1. The system allows comment creation only when the authenticated user has permission to view and operate on the target ticket according to the ticket's role-based access rules.
2. If a user without ticket access attempts to create a comment, the request is rejected with an authorization error and no ticket metadata or existing comments are returned in the response.
3. Each successful comment submission stores the comment against the correct ticket with the author identity, created timestamp, and action type captured in the audit history.
4. Comment retrieval for a ticket returns comments only to users who are authorized to access that ticket, and hidden comments are not exposed through list counts or payload fields.
5. The system validates comment content before persistence by rejecting empty or whitespace-only entries and preserving the ticket's existing comment history unchanged on validation failure.
- **[3924]** As an IT Support Agent, I want to resolve assigned tickets so that completed issues are recorded and removed from my active work queue (5 pts)
  Acceptance:
1. System allows an authorized IT Support Agent to change a ticket status to resolved only when the ticket is assigned and in an eligible pre-resolution status
2. System requires resolution details to be captured and stores the resolving agent, resolution timestamp, and final status in the ticket audit history
3. System removes resolved tickets from the agent's active work queue and displays the updated resolved status in ticket tracking views
4. User can resolve assigned tickets so that completed issues are recorded and removed from my active work queue through the documented happy-path flow
5. System validates required inputs before completing "resolve assigned tickets so that completed issues are recorded and removed from my active work queue"
- **[3930]** As an IT Support Agent, I want the system to validate resolution eligibility before resolving a ticket so that invalid or incomplete ticket closures do not violate support governance rules (3 pts)
  Acceptance:
1. When an agent attempts to resolve a ticket without all required resolution fields completed, the system blocks the action and identifies each missing or invalid field in the response or UI.
2. The system allows resolution only when the ticket is in a resolvable lifecycle state and rejects resolution attempts for statuses such as New, Closed, or Cancelled without changing stored ticket data.
3. Before applying a resolution, the system verifies that the acting user has ticket resolution permission for the ticket and returns an authorization error for unauthorized users without exposing restricted ticket details.
4. If resolution validation passes, the ticket status is updated to Resolved in a single transaction and the resolution details are saved together with the resolving agent and resolution timestamp.
5. Each blocked resolution attempt returns a clear failure reason that can be surfaced in the ticket interface and recorded in application logs for support troubleshooting.
- **[3938]** As an IT Manager, I want resolved tickets to retain complete audit history so that support operations remain traceable for governance, review, and compliance needs (3 pts)
  Acceptance:
1. When a ticket is resolved, the system creates an immutable audit record containing at minimum the ticket identifier, previous status, new status, acting agent identifier, and resolution timestamp.
2. The audit trail for a resolved ticket continues to include prior comments, assignment changes, and status transitions so the full handling sequence can be reviewed after resolution.
3. Authorized managerial users can access the audit history for a resolved ticket and view entries in chronological order with actor, action, and timestamp details.
4. Users without permission to view ticket audit data are denied access to resolved ticket history and do not receive restricted audit content in the response.
5. If a resolution action fails before completion, the system does not create a misleading final audit entry that indicates the ticket was resolved.
- **[3946]** As an Employee, I want to create a support ticket so that my issue enters the IT Help Desk lifecycle with the correct initial status (5 pts)
  Acceptance:
1. When an authenticated employee submits a description, a valid category, and a valid priority, the system creates one support ticket record with a unique ticket identifier, requester association, created timestamp, and initial lifecycle status.
2. If description, category, or priority is missing or invalid at submission time, the system does not create a ticket, preserves the entered values except rejected files, and returns field-level validation messages identifying each failed input.
3. When an attachment is included, the system accepts the file only if its size, file type, and category-based rules are satisfied; otherwise the submission is blocked and the response identifies the file validation failure.
4. On successful creation, the system starts SLA tracking using the ticket created timestamp and stores an audit history entry containing ticket identifier, actor, operation type, initial status, and submitted field values.
5. After a successful submission, the confirmation response displays the new ticket reference, current status, and submitted summary details so the employee can track the request later.
- **[3955]** As an IT Support Agent, I want to investigate a support ticket and record updates so that the issue can progress through controlled lifecycle states (5 pts)
  Acceptance:
1. System allows an authorized IT Support Agent to move an eligible support ticket into investigation and record timestamped comments or updates as part of the working history.
2. System prevents invalid investigation transitions or unauthorized updates and preserves the existing ticket state when lifecycle or access rules are not met.
3. System stores each investigation action in chronological audit history with actor, timestamp, status transition, and update content for governance and operational review.
4. System displays the current ticket category in the support ticket details for authorized IT Support Agents
5. System allows an authorized IT Support Agent to update the ticket category using valid configured category values only
6. System records the updated ticket category on the support ticket and preserves an audit trail of the category change with timestamp and actor
7. System stores an audit history record whenever a support ticket is created, updated, assigned, commented on, resolved, or closed
8. System records the authenticated actor, ticket identifier, event timestamp, operation type, and changed field values for each ticket audit event
9. System stores ticket audit history as immutable records so previously saved audit entries cannot be edited or deleted by support roles
- **[3958]** As an IT Support Agent, I want to resolve a support ticket after investigation so that the issue outcome is documented before closure (3 pts)
  Acceptance:
1. The system allows POST /api/tickets/{ticketId}/resolve only when the authenticated user has ticket resolution permission and the ticket status is an eligible investigation status.
2. The system rejects a resolve request when resolution details are blank, whitespace-only, or omitted, returns a validation error, and preserves the ticket's prior status.
3. The system rejects a resolve request when no prior investigation activity exists in ticket history or comments for the ticket and does not create a ResolutionDetail record.
4. On a successful resolve request, the system stores the resolution details, updates the ticket status to Resolved, and records prior status, resulting status, actor, and timestamp in AuditHistory.
5. If the ticket was changed by another update after the agent loaded it, the resolve request fails with a concurrency error and no partial status or audit changes are committed.
- **[3966]** As an IT Support Agent, I want to close a resolved support ticket so that the lifecycle is completed with a final auditable status (3 pts)
  Acceptance:
1. System allows an authorized IT Support Agent to close a support ticket only after the ticket is in resolved status and closure prerequisites are satisfied.
2. System prevents closure of unresolved tickets or closure attempts by unauthorized roles and leaves the ticket in its prior lifecycle state.
3. System records the final closure event with actor, timestamp, prior status, final status, and closure details in audit history and includes the closed status in lifecycle reporting.
4. User can close a resolved support ticket so that the lifecycle is completed with a final auditable status through the documented happy-path flow
5. System validates required inputs before completing "close a resolved support ticket so that the lifecycle is completed with a final auditable status"
- **[3971]** As a System Administrator, I want to manage ticket category values so that the help desk uses controlled and current classification options (5 pts)
  Acceptance:
1. The administrative category management interface allows an authorized System Administrator to create a new ticket category with required fields, edit an existing category, and deactivate an active category.
2. Only users with the configured administrator role for category governance can access category maintenance endpoints or screens; unauthorized users receive an access-denied response and cannot change category data.
3. The system validates category maintenance inputs by rejecting blank category names, duplicate active category values, and update requests for categories that do not exist.
4. Deactivating a ticket category removes it from employee category selection for new ticket creation while preserving the value on existing tickets already associated with that category.
5. Every create, update, and deactivate action on a ticket category writes an audit history entry containing the category identifier or value, action type, actor identity, timestamp, and changed fields or before/after values where applicable.
- **[3980]** As an Employee, I want to select a priority when creating a support ticket so that the IT Support team can respond based on urgency (3 pts)
  Acceptance:
1. The ticket creation form displays a required priority field populated only with the approved priority values configured for support tickets before submission is allowed.
2. When an employee submits a new ticket with a valid priority, the system persists the selected priority on the ticket record and shows the same value on the submitted ticket details view.
3. If the priority field is blank, removed from the request payload, or contains a value outside the approved list, the system rejects ticket creation and returns a clear validation message identifying the priority issue.
4. The priority selected during ticket creation is included in the ticket creation API request and response payloads using a consistent field name so downstream ticket workflow processing can consume it.
5. A signed-in employee can set priority only during ticket creation for their new ticket, and the system does not expose hidden or unauthorized priority values in the UI or API response.
- **[3988]** As an IT Support Agent, I want to update ticket priority when business impact changes so that active work reflects the current urgency (5 pts)
  Acceptance:
1. System allows authorized IT Support Agents to change ticket priority using approved values during the active ticket lifecycle.
2. System blocks priority changes for users without the required ticket management permission and preserves least-privilege access control.
3. System records each priority change in audit history with old priority, new priority, actor identity, and timestamp for compliance review.
4. User can update ticket priority when business impact changes so that active work reflects the current urgency through the documented happy-path flow
5. System validates required inputs before completing "update ticket priority when business impact changes so that active work reflects the current urgency"
- **[3991]** As an IT Manager, I want to view agent workload dashboards so that I can balance assignments across support agents (5 pts)
  Acceptance:
1. When an authorized IT Manager opens the workload dashboard, the system displays current assigned ticket counts aggregated by queue and by current responsible support agent using the latest valid assignment record.
2. The dashboard shows per-agent workload breakdowns that include ticket priority distribution, ticket status distribution, and overdue ticket counts derived from the filtered ticket set.
3. If the IT Manager applies team, queue, category, and date range filters, the system refreshes the summary and drill-down data using only records within the authorized reporting scope and rejects invalid date ranges.
4. When the IT Manager drills into a selected queue or agent, the system returns detailed assignment distribution data that is consistent with the corresponding summary totals for the same filters.
5. If a user without the required manager reporting permission or outside the permitted organizational scope requests the dashboard or drill-down endpoint, the system denies access and records the attempt in audit history.
- **[4000]** As an IT Manager, I want to review SLA performance and overdue issue reports so that I can intervene before service targets are missed (5 pts)
  Acceptance:
1. System provides SLA performance reports and overdue issue reports that calculate compliant, at-risk, breached, and overdue tickets using standardized priority and category driven SLA rules
2. System allows the IT Manager to filter SLA and overdue reports by queue, category, priority, and date range and returns results with consistent calculation logic across ticket lifecycle states
3. System records report access and export activity in the audit history and limits report visibility to authorized IT Managers under least-privilege access policies
4. User can review SLA performance and overdue issue reports so that I can intervene before service targets are missed through the documented happy-path flow
5. System validates required inputs before completing "review SLA performance and overdue issue reports so that I can intervene before service targets are missed"
- **[4111]** As an IT Support Manager, I want to review ticket performance and SLA reports so that I can measure service delivery against operational targets (5 pts)
  Acceptance:
1. The system generates ticket performance reports for a selected reporting period showing response time, resolution time, SLA attainment percentage, backlog aging, and workload trend totals using ticket lifecycle timestamps and SLA calculation records.
2. An authorized IT Support Manager can filter report results by reporting period, ticket category, priority, status, assignee, and organizational scope, and the returned dataset reflects only records matching all selected filters.
3. Report calculations use only controlled ticket lifecycle states, assignment history, and auditable SLA metric data; tickets with incomplete required timestamps are excluded from time-based aggregates and counted in a visible data-quality exception total.
4. The system allows export of the filtered report in a supported file format and the exported rows and summary values match the report currently displayed on screen at the time of export.
5. If a user without reporting permission attempts to open or export SLA performance reporting, the system denies access and returns an authorization error without exposing ticket metrics or underlying ticket data.
- **[4003]** As an IT Manager, I want to export dashboard and report outputs so that I can share support metrics for operational review (3 pts)
  Acceptance:
1. Only users with the IT Manager role and report export permission can invoke dashboard or report export actions, and unauthorized users receive an access denied response without a file being generated.
2. An export generated from a dashboard or report uses the active on-screen filters and returns only records and summary values within the requesting manager's authorized reporting scope.
3. Exported output includes approved support metrics for ticket status, agent workload, SLA performance, overdue issues, and related report columns while excluding or masking restricted ticket content based on role rules.
4. Each successful export writes an immutable audit entry containing the export type, report or dashboard identifier, applied filters, requesting user, timestamp, and outcome status.
5. For approved reporting ranges, the system returns the export file within 10 seconds over encrypted transport and surfaces a user-visible error message if generation fails or times out.
- **[4014]** As an IT Audit Manager, I want report access and reporting actions to be fully auditable so that support performance reviews meet governance and compliance requirements (3 pts)
  Acceptance:
1. When an authenticated user opens a support performance report, the system writes an immutable audit entry containing userId, role, reportId or reportName, actionType "REPORT_VIEW", timestamp, and request correlation id.
2. When a user runs a report with filters, the system records the executed filter set in the audit entry, including filter names and submitted values, and rejects logging of malformed or unsupported filter parameters.
3. When a user exports a support performance report, the system records the export action with exportFormat, reportId, requesting userId, timestamp, and outcome status without exposing unauthorized users to export capability.
4. When a user creates, updates, or deletes scheduled report distribution settings, the system records the administration event with schedule identifier, action performed, initiating userId, and timestamp.
5. Audit records for report access and reporting actions are read-only after creation, and authorized audit reviewers can retrieve them for compliance review without any ability to edit or delete historical entries.
- **[4021]** As an IT Manager, I want ticket activity notifications to be controlled and auditable so that support communications comply with internal security and governance requirements (5 pts)
  Acceptance:
1. System records each ticket activity notification in an audit trail with ticket reference, recipient, activity type, delivery outcome, and timestamp
2. System enforces role-based notification access controls so only authorized employees, assigned agents, and approved support roles can view ticket activity details
3. System captures notification delivery failures for operational review while preserving a complete audit record of the original ticket activity event
4. User can I want ticket activity notifications to be controlled and auditable so that support communications comply with internal security and governance requirements through the documented happy-path flow
5. System validates required inputs before completing "I want ticket activity notifications to be controlled and auditable so that support communications comply with internal security and governance requirements"
- **[4134]** As a System Administrator, I want to configure notification integration settings so that ticket activity notifications can be managed without changing code (5 pts)
  Acceptance:
1. Only authenticated users with administrative notification configuration permission can create, edit, activate, deactivate, or view sensitive notification integration settings; unauthorized users receive an access denied response and no changes are persisted.
2. An administrator can configure at least one notification integration channel with channel-specific settings, select which ticket activity events map to that channel, and save the configuration only when all required fields pass validation.
3. Sensitive integration secrets such as API keys, tokens, or webhook credentials are never returned in plain text after save, are stored encrypted at rest, and are redacted in audit and API response payloads.
4. Each successful configuration change creates an audit entry containing the acting user, timestamp, integration channel, fields changed, previous value summary, new value summary, and resulting status of the configuration.
5. The system retrieves saved notification integration settings accurately after page reload or API re-fetch, preserving event mappings, channel status, and non-secret delivery settings without requiring application code changes or restart.
- **[4142]** As an IT Manager, I want notification delivery failures to be monitored and retried so that important ticket activity updates are not missed (5 pts)
  Acceptance:
1. System retries failed ticket activity notifications according to configured retry rules and records each attempt outcome.
2. System raises a monitored error state when notification delivery cannot be completed after retry attempts so support teams can intervene.
3. System retains an auditable history of notification failures and recoveries linked to the affected support ticket and integration channel.
4. User can I want notification delivery failures to be monitored and retried so that important ticket activity updates are not missed through the documented happy-path flow
5. System validates required inputs before completing "I want notification delivery failures to be monitored and retried so that important ticket activity updates are not missed"
- **[4024]** As an Employee, I want to access only my own support tickets so that my issue information remains private and secure (3 pts)
  Acceptance:
1. When an authenticated Employee opens the ticket list, the system displays only tickets where the ticket creator matches the authenticated employee identity and excludes tickets created by other employees.
2. When an authenticated Employee opens a ticket detail page or detail API for a ticket they created, the system returns that ticket's details, comments, status history, and attachment references according to normal employee permissions.
3. When an authenticated Employee attempts to access a ticket not created by them through a direct URL, API request, or modified ticket identifier, the system returns an authorization failure and does not expose ticket title, description, status, comments, history, or attachment metadata.
4. When an authenticated Employee attempts to update, comment on, or retrieve an attachment for a ticket not created by them, the system blocks the operation even if the ticket identifier is valid.
5. For each denied access attempt to another employee's ticket, the system writes an audit record containing the authenticated employee identity, requested ticket identifier, access channel, timestamp, and denied outcome.
- **[4032]** As a Support Agent, I want to access assigned and permitted support tickets so that I can investigate and resolve issues within SLA targets (5 pts)
  Acceptance:
1. System allows a support agent to view, update, comment on, and progress support tickets only when the ticket is assigned to the agent or otherwise permitted by configured role rules.
2. System prevents a support agent from performing restricted ticket actions outside assigned or authorized scope, including unauthorized reassignment, closure, or access to protected attachments.
3. System records support agent ticket actions and denied authorization attempts in audit history with ticket status change details, actor role, and timestamp for governance tracking.
4. User can access assigned and permitted support tickets so that I can investigate and resolve issues within SLA targets through the documented happy-path flow
5. System validates required inputs before completing "access assigned and permitted support tickets so that I can investigate and resolve issues within SLA targets"
- **[4037]** As a Manager, I want to access team support tickets and oversight information so that I can monitor workload, escalations, and SLA adherence (5 pts)
  Acceptance:
1. The system displays only tickets within the authenticated manager's authorized team or operational scope, including ticket ID, assignee, priority, status, SLA state, and escalation indicator, and excludes tickets outside that scope.
2. The manager oversight view provides workload totals, overdue ticket counts, breached or at-risk SLA counts, and escalated ticket counts derived from current ticket data without requiring the manager to be the ticket owner.
3. A manager can open ticket detail for any ticket in their authorized scope and view assignment history, current status, SLA timestamps or remaining time, and escalation state, but cannot access user-role administration functions from this workflow.
4. If a manager attempts to access a ticket, team, or oversight endpoint outside their authorized scope, the request is denied with an authorization error and no restricted ticket data is returned.
5. Each access to team ticket lists, oversight summaries, and ticket detail pages by a manager is recorded in audit history with manager identity, accessed resource or filter context, action type, and timestamp.
- **[4046]** As an Administrator, I want to assign and maintain user roles so that access for employees, support agents, managers, and administrators is controlled centrally (5 pts)
  Acceptance:
1. System allows only an administrator to assign, change, or revoke role memberships for employees, support agents, managers, and administrators through authorized administration functions.
2. System validates role updates before saving and prevents conflicting or unauthorized role changes that violate configured access control policy or segregation of duties expectations.
3. System stores each role assignment change with previous value, new value, administrator identity, and timestamp in immutable audit history for compliance and security review.
4. User can assign and maintain user roles so that access for employees, support agents, managers, and administrators is controlled centrally through the documented happy-path flow
5. System validates required inputs before completing "assign and maintain user roles so that access for employees, support agents, managers, and administrators is controlled centrally"
- **[4051]** As an Administrator, I want role-based permissions enforced across the user interface and APIs so that unauthorized actions are consistently blocked (5 pts)
  Acceptance:
1. When an authenticated user opens ticket, reporting, configuration, or administration screens, the UI displays only actions and navigation items permitted by that user's assigned role and hides restricted controls before interaction.
2. When a restricted operation is attempted through a direct API call using a valid authenticated session, the API returns a forbidden response without executing the underlying ticket, user, role, or configuration change.
3. If a client request is tampered with to submit a disallowed action that was hidden in the UI, server-side authorization still rejects the request and no data is created, updated, deleted, or reassigned.
4. Each authorization denial records an audit event containing the authenticated user identifier, role, target resource or action, timestamp, and outcome code so security review can trace unauthorized attempts.
5. Authorization decisions for the same action are consistent between UI-driven requests and direct API requests for the same user role, producing matching allow or deny outcomes across both access paths.
- **[4095]** As an Administrator, I want to manage user group roles so that role-based access remains accurate across employee, support agent, manager, and administrator accounts (5 pts)
  Acceptance:
1. System allows an administrator to assign and update role membership for employee, support agent, manager, and administrator user groups
2. System validates that only authorized administrators can change role assignments and prevents unauthorized role administration attempts
3. System applies updated role permissions to subsequent user access requests without exposing functions outside the assigned role scope
4. User can manage user group roles so that role-based access remains accurate across employee, support agent, manager, and administrator accounts through the documented happy-path flow
5. System validates required inputs before completing "manage user group roles so that role-based access remains accurate across employee, support agent, manager, and administrator accounts"
- **[4100]** As an Administrator, I want access control activity to be auditable so that role changes and denied actions can be reviewed for governance and security compliance (3 pts)
  Acceptance:
1. When an authorized administrator changes a user's role, the system stores an audit record containing the acting administrator identifier, affected account identifier, previous role, new role, event type, and server-generated timestamp.
2. When access is denied to a restricted ticket function, administrative page, or protected API, the system stores a denied-action audit record containing the requesting user identifier when available, attempted resource or function, action type, denial outcome, denial reason or policy basis, and server-generated timestamp.
3. Audit records created for role changes and denied actions are persisted in the system audit store and are not editable through standard administrative role management screens or ticket operation screens.
4. If a denied request targets a protected API without a valid authenticated session, the system still records the denied event with the available request context and returns the appropriate authorization response without exposing protected data.
5. Access control audit records exclude ticket attachment contents, comment bodies, passwords, tokens, and other secret values while retaining enough metadata for governance review.
- **[4075]** As an Employee, I want to access only my permitted ticket functions so that I can submit and track support tickets without viewing restricted support operations (3 pts)
  Acceptance:
1. Authenticated users with the Employee role can access ticket creation, their own ticket list, ticket detail for tickets they submitted, and submitted ticket status/history views, while support dashboard, assignment, SLA governance, reporting export, and administration routes return access denied.
2. When an Employee requests a ticket record by direct URL or API using a ticketId not submitted by that user, the system returns a forbidden response and does not expose ticket summary, comments, attachments, assignment data, or audit metadata.
3. Employee users cannot execute restricted ticket actions including assignment, reassignment, priority governance updates, lifecycle transitions reserved for support staff, or role/configuration administration, and restricted action controls are not rendered in the UI.
4. Employee access to comments and attachments is limited to content associated with tickets the employee is permitted to view, and attempts to open attachment download endpoints or comment retrieval endpoints for other users' tickets are blocked and logged.
5. Role-based permission checks are enforced consistently after enterprise authentication for both UI navigation and backend API calls, so changing client-side requests or hidden form values cannot elevate Employee capabilities.
- **[4083]** As a Support Agent, I want role-based access to assigned ticket operations so that I can investigate, update, and resolve support tickets within my authorized scope (5 pts)
  Acceptance:
1. System allows a support agent to view, update, comment on, and resolve tickets within the agent's authorized queue or assignment scope
2. System prevents a support agent from performing administrator-only role management actions or accessing restricted configuration functions
3. System enforces role-based access consistently across user interface actions and API requests for ticket status updates, comments, and attachment handling
4. User can I want role-based access to assigned ticket operations so that I can investigate, update, and resolve support tickets within my authorized scope through the documented happy-path flow
5. System validates required inputs before completing "I want role-based access to assigned ticket operations so that I can investigate, update, and resolve support tickets within my authorized scope"
- **[4086]** As a Manager, I want elevated access to team ticket oversight functions so that I can monitor workload, assignments, and controlled ticket actions for my support area (5 pts)
  Acceptance:
1. When a user with the Manager role opens oversight functions, the system displays ticket workload, assignment details, and permitted controlled ticket actions only for the manager's authorized support area.
2. The system denies manager access to administrator-only capabilities, including role maintenance, user role administration, and system-wide security configuration pages or APIs.
3. For ticket detail and assignment history views, the system returns data only for tickets within the manager's authorized team, queue, or support-area scope and excludes unauthorized records.
4. If a manager initiates a controlled ticket action, the system validates both role permission and ticket scope before applying the change and records the action in the audit trail.
5. If a manager requests a dashboard, ticket, or action outside authorized scope, the system returns an authorization error without exposing restricted ticket data or hidden functions.
- **[4060]** As a System Administrator, I want to manage support user accounts so that only approved employees and support staff can access the IT Help Desk Management System (5 pts)
  Acceptance:
1. System allows System Administrators to create, update, activate, and deactivate user accounts with required identity fields while maintaining a unique account for each employee or support staff member
2. System prevents unauthorized roles from accessing user administration functions and logs denied access attempts for security review
3. System stores an audit trail for each user account change including the administrator, timestamp, and before-and-after values to support compliance and internal governance
4. User can manage support user accounts so that only approved employees and support staff can access the IT Help Desk Management System through the documented happy-path flow
5. System validates required inputs before completing "manage support user accounts so that only approved employees and support staff can access the IT Help Desk Management System"
- **[4065]** As a System Administrator, I want to manage role definitions and role assignments so that ticket access and administrative actions follow least-privilege controls (5 pts)
  Acceptance:
1. Only authenticated users with the System Administrator role can create a new role, edit an existing role's permission mappings, or assign and unassign roles to user accounts.
2. When a role is saved, the system requires a unique role name and persists explicit permission mappings for ticket operations, dashboards, reports, and administrative configuration areas.
3. After a role assignment change is committed, the assigned user's effective permissions are enforced on both protected UI actions and backend APIs without granting access outside the selected role mappings.
4. The system prevents deletion or deactivation of a role that is marked as system-default or currently required for platform administration, and returns a clear validation message.
5. Each role definition change and role assignment change writes an immutable audit record containing actor user ID, affected role or account, action type, previous values, new values, and timestamp.
- **[4070]** As a System Administrator, I want to manage system configuration values so that ticket processing rules and operational settings can be controlled without code changes (5 pts)
  Acceptance:
1. Only authenticated users with the System Administrator role can add, update, activate, or deactivate governed configuration values through the configuration administration interface and APIs.
2. When a configuration value is created or updated, the system validates required fields, value format, allowed configuration type, and uniqueness within its configuration group before saving.
3. An inactive configuration value is not available for selection in dependent administrative screens or ticket entry forms, while active values are returned consistently to those consumers.
4. The system preserves existing ticket records and historical reporting context when a configuration value is updated or deactivated, so prior tickets continue to reference their previously stored values.
5. Each configuration change writes an immutable audit record containing actor user ID, configuration group, key or code, action type, previous value, new value, status, and timestamp.
- **[4166]** As an IT Manager, I want to control access, assignments, and SLA visibility so that support operations remain secure, accountable, and timely (5 pts)
  Acceptance:
1. The system enforces role-based permissions for ticket view, assignment, update, resolution, and closure actions in both the user interface and API so unauthorized roles cannot perform restricted ticket operations.
2. Only authorized roles can assign or reassign a ticket, and the assignee selector returns only active eligible support personnel who are permitted to own the ticket.
3. For users with permitted access, each ticket detail view shows the current assignee, reassignment history, due time, and SLA state, and the ticket list view shows SLA state and due time indicators.
4. The system calculates SLA status from configured target times and current elapsed time and displays one of the supported states on-track, at-risk, or breached consistently in ticket list and detail views.
5. Each assignment change and each blocked access-sensitive operation creates an audit record containing actor, action type, affected ticket identifier, timestamp, and relevant before-and-after assignment details when applicable.
- **[4175]** As an IT Manager, I want to view centralized ticket dashboards and reports so that I can monitor workload, status, and SLA performance across support operations (5 pts)
  Acceptance:
1. When an authorized IT Manager opens the dashboard, the system displays centralized widgets for open, in-progress, resolved, closed, overdue, breached, and total ticket counts using centralized ticket and SLA data.
2. When the manager applies a date range, category, priority, status, or assignee filter, the same filter set is applied to all dashboard summaries, workload views, SLA report sections, and drill-down ticket lists.
3. The dashboard displays agent workload metrics that show assigned ticket counts and status distribution for each assignee or team scope the manager is permitted to view.
4. When the manager selects a summary widget or chart segment, the system opens the underlying ticket list and the returned records correspond to the selected metric and active filters.
5. If a user without the required manager reporting permission attempts to access the dashboard or drill-down endpoint, the system denies access and does not expose ticket metrics or ticket lists.
- **[4108]** As an IT Manager, I want to view a dashboard of ticket lifecycle status and overdue work so that I can monitor support operations and intervene on bottlenecks (5 pts)
  Acceptance:
1. System displays a dashboard with ticket counts by lifecycle status, overdue tickets, and assigned workload based on current Support Ticket, Assignment, Ticket Priority, and Ticket Status data
2. System allows filtering the dashboard by category, priority, assignee, and date range and updates all dashboard widgets using the same filter context
3. System enforces role-based access so the IT Manager can view authorized operational metrics without exposing restricted ticket details or attachment content outside policy
4. User can view a dashboard of ticket lifecycle status and overdue work so that I can monitor support operations and intervene on bottlenecks through the documented happy-path flow
5. System validates required inputs before completing "view a dashboard of ticket lifecycle status and overdue work so that I can monitor support operations and intervene on bottlenecks"
- **[4120]** As a System Administrator, I want to control access to dashboards and reporting and audit their usage so that sensitive ticket information remains protected and traceable (5 pts)
  Acceptance:
1. System restricts dashboard, report, and export access using role-based permissions for Business User, IT Support Agent, IT Manager, and administrative roles in line with least-privilege policy
2. System records immutable audit entries for dashboard access, report execution, export activity, and permission-controlled viewing of sensitive ticket data with user, timestamp, action, and source context
3. System secures reporting requests and responses through authenticated access, encrypted transport, and validated inputs so reporting functions comply with internal security and audit requirements
4. User can control access to dashboards and reporting and audit their usage so that sensitive ticket information remains protected and traceable through the documented happy-path flow
5. System validates required inputs before completing "control access to dashboards and reporting and audit their usage so that sensitive ticket information remains protected and traceable"
- **[4145]** As an Employee, I want to sign in using enterprise identity so that I can securely access my support tickets without separate credentials (5 pts)
  Acceptance:
1. When an employee selects sign-in, the application redirects to the configured enterprise identity provider and completes authentication only when a valid identity response containing the required user identifier is returned.
2. After successful enterprise authentication, the system creates an authenticated session for an active employee account and grants access to employee-permitted ticket pages without requiring a local password.
3. If enterprise authentication fails, the identity response is missing required assertions, or the mapped employee account is inactive, the system denies access, shows a non-sensitive error message, and does not create a session.
4. Successful and failed sign-in attempts are recorded with timestamp, mapped employee identifier, authentication outcome, and failure reason code when applicable.
5. Unauthenticated users who request employee ticket pages are redirected to the enterprise sign-in flow instead of receiving ticket data or partial page content.
- **[4154]** As an IT Support Agent, I want role-based access to assigned ticket functions so that I can process support work without exposing unauthorized data (5 pts)
  Acceptance:
1. System allows an authenticated IT Support Agent to access only the ticket queues, ticket details, comments, and status actions permitted by the assigned role
2. System prevents an IT Support Agent from performing unauthorized administrative or manager-only actions and returns an access denied response without exposing restricted data
3. System records ticket access, assignment actions, status changes, and comments performed by the IT Support Agent in the audit history
4. User can I want role-based access to assigned ticket functions so that I can process support work without exposing unauthorized data through the documented happy-path flow
5. System validates required inputs before completing "I want role-based access to assigned ticket functions so that I can process support work without exposing unauthorized data"
- **[4157]** As an IT Manager, I want to manage role-based access permissions so that support operations follow governance and audit requirements (5 pts)
  Acceptance:
1. An authenticated user with the IT Manager role can assign, replace, or revoke application roles for employee and IT Support Agent accounts through the role administration interface and corresponding secured API.
2. The system blocks role changes when the acting user lacks the required manager permission, when the target account is inactive, or when the requested role combination violates configured segregation-of-duties rules.
3. Each successful role change persists the affected account identifier, previous role set, new role set, acting manager identifier, timestamp, and audit reference in the audit history store.
4. The role management view displays the current assigned roles for the selected employee or IT Support Agent before a change is submitted, and refreshes to the saved role state after a successful update.
5. If a role update request fails validation or authorization, the system does not persist any access change and returns a clear error message that identifies the blocking condition.
- **[3688]** Ticket SLA Calculation and Lifecycle Tracking
- **[4210]** As a user, I want to use SLA Monitoring and Breach Visibility so that I can benefit from its core functionality (5 pts)
  Acceptance:
1. The SLA monitoring view displays a ticket list containing ticket ID, summary, priority, current status, assignment, SLA due date/time, and an SLA state indicator for each returned record.
2. When the user filters by SLA state, priority, status, or assignment, the system returns only records matching the selected criteria and updates the visible result count accordingly.
3. Tickets whose SLA target time has passed are visually identified as overdue or breached based on the persisted SLA calculation outcome, and they are distinguishable from tickets still within SLA.
4. Only users with permission to access ticket monitoring can open the view or retrieve its data; unauthorized access attempts are denied without exposing ticket details.
5. The monitoring view supports sorting by at least SLA due date/time and priority, and the initial and filtered result loads complete within the defined performance threshold for standard operational volume.
6. When the SLA Monitoring and Breach Visibility view fails to load monitoring data due to a server or network error, the UI displays a specific error state with a retry action and does not show stale breach or overdue results as current data.
7. When the user enters an invalid monitoring filter combination such as an end date earlier than the start date, the request is not submitted and the UI highlights the invalid field values with corrective guidance.
8. When the user selects retry after a failed monitoring data load, the system issues one new data request and replaces the error state with refreshed SLA monitoring results if the retry succeeds.
9. When the authenticated user lacks permission to view SLA monitoring data, the system returns an access-denied response, hides ticket-level monitoring details, and shows a non-technical authorization message.
10. Each failed SLA monitoring request is recorded in application logs with correlationId, failure category, request context, and timestamp so support teams can troubleshoot the incident.
- **[4225]** As a user, I want to configure SLA Monitoring and Breach Visibility settings so that I can customize it to my needs (3 pts)
  Acceptance:
1. Feature functionality works as described
2. User can access the feature through the UI
3. Feature handles edge cases gracefully
4. Feature is properly tested
5. Feature meets performance requirements
- **[4201]** As an IT Support Agent, I want the system to calculate SLA targets for each support ticket so that I can work against accurate response and resolution deadlines (5 pts)
  Acceptance:
1. When a ticket is created with required fields including category and priority, the system shall calculate and store response and resolution SLA target timestamps using the matching SLA policy.
2. When an agent views a ticket in list or detail view, the system shall display SLA due dates, remaining time, and current status such as on track, paused, met, or breached.
3. When ticket priority or category is updated during processing, the system shall apply configured recalculation rules and persist an audit entry showing the old and new SLA values or the reason recalculation was not applied.
4. When a ticket enters and exits a configured pause status, the system shall stop and restart the applicable SLA clock without losing prior elapsed time and shall record the timing events.
5. When a ticket is resolved and closed, the system shall persist final response and resolution SLA outcomes so they are available for ticket history, reporting, and overdue analysis.
6. The system shall provide a manager dashboard that displays ticket counts by SLA status including on track, near breach, paused, and breached using persisted ticket SLA data.
7. The system shall allow managers to filter SLA monitoring results by date range, ticket status, assignee, category, priority, and overdue condition, with the same filters applied consistently to summaries and drill-down lists.
8. When a manager drills into breached or near-breach tickets, the system shall display ticket-level details including assignee, current status, SLA deadlines, remaining time or overdue duration, and key lifecycle timestamps.
9. The system shall present SLA performance summaries for a selected period, including counts and rates for met and breached response and resolution SLAs.
10. Access to SLA monitoring dashboards and drill-down data shall be restricted to authorized manager roles, and dashboard results shall load within agreed performance thresholds for standard data volumes.
- **[4233]** System Design - 9/25/2026
- **[4234]** System Design - 9/25/2026



## Implementation Instructions

1. Read the local handoff files under `specs/.devx/`: project context, current state, implementation check, and this feature's change map
2. When Golden Repo guidance exists (under `specs/.devx/guidance/` or the ui-design golden files), you MUST read it before planning and reconcile each convention — apply it, or mark it not-applicable-to-this-stack with a one-line rationale — and include a short "Golden Repo Reconciliation" note in your output (verified local patterns override golden guidance on conflict)
3. Inspect existing code first using the change map; verify candidate files, routes, components, services, tests, and shared modules in the actual workspace
4. Create an implementation plan that prefers modifying existing code over creating parallel implementations
5. Read `specs/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more/specs.md` thoroughly before writing any code
6. Read this feature's Open Questions (`specs/.devx/features.json` `openQuestions[]`, or the `## Open Questions` section in `specs/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more/specs.md`). Interactive run: surface the consolidated list, ask the human user for a decision before coding, apply their answer, and record the decision plus a one-line rationale in `specs/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more/assumptions.md`. Unattended run: choose a reasonable assumption and record it plus a rationale in `specs/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more/assumptions.md` — never assume silently. A feature held at `needs-clarification` (tracker `NEEDS_CLARIFICATION`) has an unresolved blocking question and must be resolved and re-enabled before implementing
7. Implement each item from `specs/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more/requirements.md` one at a time
8. Validate each checklist item as you implement it

## Constraints

- Do not deviate from the specification
- Do not add features or behaviors not in the spec
- Follow the project's established patterns and conventions
- Existing code patterns override generic guidance when they conflict
- Do not rely on Astra or live Golden Repo access during IDE implementation
- Each implementation acceptance item in requirements.md must be satisfied

## Validation

After implementation, verify:
- [ ] Local handoff files were read before planning
- [ ] When Golden Repo artifacts exist, they were read and a "Golden Repo Reconciliation" note was produced (each convention Applied or Not-applicable-to-this-stack with rationale)
- [ ] Existing code was inspected before editing
- [ ] Open Questions were reviewed; each was surfaced/asked (interactive) or recorded with a decision + rationale in `specs/as-an-employee-i-want-to-receive-notifications-when-my-support-ticket-changes-so-that-i-stay-informed-about-progress-and-required-actions-as-an-it-support-agent-i-want-to-receive-notifications-when-a-support-ticket-is-assigned-or-reassigned-so-that-i-can-act-on-owned-work-without-delay-as-an-it-support-agent-i-want-ticket-lifecycle-events-to-trigger-notifications-so-that-stakeholders-are-informed-of-ticket-activity-83-more/assumptions.md` (unattended), and no blocking Open Question was left unresolved and unrecorded (never assumed silently)
- [ ] All implementation acceptance items in `requirements.md` are satisfied
- [ ] All user scenarios from `specs.md` work correctly
- [ ] No extra features were added beyond the spec
- [ ] Code follows project conventions
